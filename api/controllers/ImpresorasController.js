/**
 * ImpresorasController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */
// api/controllers/ImpresorasController.js
const Impresoras = require('../models/Impresoras');

module.exports = {
    // buscarImpresoras: async function (req, res) {
    //     let consulta = `SELECT * FROM impresoras`;
    //     try {
    //         const resultados = await Impresoras.getDatastore().sendNativeQuery(consulta);
    //         res.json(resultados.rows);
    //     } catch (error) {
    //         console.error(error);
    //         res.serverError();
    //     }
    // }


};

